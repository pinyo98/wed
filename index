<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>⚽ Multi Live Football Streaming</title>
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      font-family: 'Prompt', sans-serif;
      background: linear-gradient(to bottom, #0d47a1, #1976d2);
      color: #fff;
      text-align: center;
      min-height: 100vh;
    }
    header {
      background: #0b3d91;
      padding: 1rem;
      font-size: 1.5rem;
      font-weight: 600;
      box-shadow: 0 2px 5px rgba(0,0,0,0.4);
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 1rem;
      padding: 1rem;
      max-width: 1400px;
      margin: 0 auto;
    }
    .match-box {
      background: rgba(0,0,0,0.3);
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 3px 10px rgba(0,0,0,0.3);
      backdrop-filter: blur(4px);
    }
    iframe {
      width: 100%;
      height: 250px;
      border: none;
    }
    h3 {
      margin: 0;
      padding: 0.6rem;
      background: rgba(255,255,255,0.1);
    }
    .live-tag {
      color: #ff5252;
      font-weight: bold;
      animation: blink 1s infinite alternate;
    }
    @keyframes blink {
      from { opacity: 1; }
      to { opacity: 0.3; }
    }
    @media (max-width: 600px) {
      iframe { height: 200px; }
      h3 { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <header>⚽ ถ่ายทอดสดหลายคู่พร้อมกัน ⚽</header>

  <div class="grid" id="liveGrid"></div>

  <script>
    // 📺 ใส่ลิงก์ถ่ายทอดสดตรงนี้ (เพิ่มได้ไม่จำกัด)
    const matches = [
      {
        teams: "ลิเวอร์พูล vs แมนฯ ซิตี้",
        link: "https://www.youtube.com/embed/tgbNymZ7vqY"
      },
      {
        teams: "อาร์เซน่อล vs เชลซี",
        link: "https://www.youtube.com/embed/aqz-KE-bpKQ"
      },
      {
        teams: "แมนฯ ยูไนเต็ด vs สเปอร์ส",
        link: "https://www.youtube.com/embed/ysz5S6PUM-U"
      },
      {
        teams: "เรอัล มาดริด vs บาร์เซโลน่า",
        link: "https://www.youtube.com/embed/X2mV3lYkHq0"
      }
    ];

    c
